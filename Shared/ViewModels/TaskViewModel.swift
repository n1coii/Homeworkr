//
// Created by Nicolas Legros on 2021-02-15.
//

import Foundation
import CoreData


class TaskViewController {
    
//    let context = NSManagedObjectContext()
//    let fetchRequest = NSFetchRequest<Task>(entityName: "Employee")
//    // Configure the request's entity, and optionally its predicate
////    fetchRequest.sortDescriptors = [NSSortDescriptor(key: "<#Sort key#>", ascending: true)]
//    let controller = NSFetchedResultsController(fetchRequest: fetchRequest, managedObjectContext: context, sectionNameKeyPath: nil, cacheName: nil)
//    do {
//        try controller.performFetch()
//    } catch {
//        fatalError("Failed to fetch entities: \(error)")
//    }

//    @FetchRequest(entity: Task.entity(), sortDescriptors: [
//        NSSortDescriptor(keyPath: \Task.due, ascending: true)])
//    var tasks: FetchedResults<Task>

//    func getTasks() -> FetchedResults<Task> {
//        return tasks
//    }

    func sortByPriority() {

    }
}
